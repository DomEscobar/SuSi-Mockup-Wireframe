import { InteractDirective } from './interact.directive';

describe('InteractDirective', () => {
  it('should create an instance', () => {
    const directive = new InteractDirective();
    expect(directive).toBeTruthy();
  });
});
